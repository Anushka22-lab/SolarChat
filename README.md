# SolarChat 🌞🤖

An AI-based chatbot for solar storm impact analysis.

## Features
- Predicts storm occurrence and severity
- Uses trained ML models
- FastAPI backend + simple frontend

## Setup
```bash
python -m venv .venv
pip install -r requirements.txt
python main.py
